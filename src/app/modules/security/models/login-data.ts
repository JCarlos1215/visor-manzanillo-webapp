/**
 * Interfaz con la estructura de datos necesarios para autentificarse.
 */
export interface LoginData {
  /**
   * Cadena con el nombre de usuario.
   */
  username: string;
  /**
   * Cadena con la contraseña del usuario.
   */
  password: string;
}
